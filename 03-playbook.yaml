- name: Install and Run Nginx
  hosts: web
  become: yes
  tasks:
  - name: Installing Nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Run Nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
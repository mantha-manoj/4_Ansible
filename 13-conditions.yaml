- name: Cheking the number
  hosts: local
  connection: local
  vars:
    Number: 10
  tasks:
  - name: Check number is less than 10 or not
    ansible.builtin.debug:
      msg: "Given Number {{ Number }} is less than 10"
    when: Number < 10